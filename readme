#CHDS : C Http Download server


服务器运行方式:

bin目录下：
编译：sudo make
运行：sudo ./CHDS [port]
清理：sudo make clean



连接方式:

连接：telnet [server addr] [port]
请求文件：GET [file name] (如果在服务器找到此文件就能下载，没找到会有提示)
退出：exit 或 quit
